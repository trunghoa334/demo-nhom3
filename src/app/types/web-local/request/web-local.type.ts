export interface CreateWebLocalRequestType {
    id: string
    isActived: boolean
    localization: string
}

export interface UpdateWebLocalRequestType {
    id: string
    isActived: boolean
    localization: string
}
